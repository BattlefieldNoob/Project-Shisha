{
  "name": "tablo-crawler",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/index",
    "test": "bun test",
    "lint": "npx eslint src/ --fix",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@project-shisha/shared-lib": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@project-shisha/shared-config": "workspace:*",
    "@types/node": "^20.11.0",
    "bun-types": "^1.3.10",
    "typescript": "^5.3.3"
  }
}
