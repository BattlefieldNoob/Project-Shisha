# Ansible

This directory contains Ansible playbooks and roles for deployment in the "Project Shisha" monorepo.

## Structure

- `inventories/` - Inventory files for different environments
- `playbooks/` - Main deployment playbooks
- `roles/` - Reusable Ansible roles