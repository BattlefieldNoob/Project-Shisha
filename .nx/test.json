{
  "$schema": "https://nx.dev/schema/nx-plugin.json",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun test",
        "cwd": "{projectRoot}"
      },
      "cache": true,
      "inputs": ["default", "^production"],
      "outputs": []
    }
  }
}
