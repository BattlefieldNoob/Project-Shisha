# Infrastructure

This directory contains infrastructure configuration for the "Project Shisha" monorepo.

## Structure

- `compose/` - Docker Compose files for shared services
- `env/` - Environment configuration files
- `docs/` - Documentation for infrastructure operations
